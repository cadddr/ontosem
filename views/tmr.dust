<div id="sentence">
  {# sentence}
    <span data-entity-id="{_id}"
      data-entity-name="{_name}">
        {_token}
    </span>
  {/ sentence}
</div>

<div class="tmr-container">

  {# tmrs}
  <div class="tmr">
    <dl>
      <dt data-entity-id="{_id}">{_key}</dt>
      <dd class="attributes">
        {# attrs}
          {> "kvpair" /}
        {/ attrs}
      </dd>
      <dd class="optional-attributes">
        {# optional}
          {> "kvpair" /}
        {/ optional}
      </dd>
    </dl>
  </div>
  {/ tmrs}
</div>
